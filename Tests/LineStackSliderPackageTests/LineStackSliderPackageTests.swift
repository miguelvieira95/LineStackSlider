import XCTest
@testable import LineStackSliderPackage

final class LineStackSliderPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LineStackSliderPackage().text, "Hello, World!")
    }
}
